#include "main.h"
/**
 * _strspn - length of string.
 * @s: char pointer.
 * @accept: string value.
 *
 * Return: number of bytes in initial segment.
 */
unsigned int _strspn(char *s, char *accept)
{
	for (accept = 0; s[accept] != '\0'; accept++)
	{
		return (sizeof(s[accept]);
				}
				}
