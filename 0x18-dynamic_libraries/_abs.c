#include "main.h"
/**
 *_abs - computes the absolute value of a number
 *@n: number to be computed
 *
 *Return: returns n or else 0
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (0);
}
