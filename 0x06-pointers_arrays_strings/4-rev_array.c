#include "main.h"
/**
 * reverse_array -  reverses the content of,
 * an array of integers.
 * @n: input value
 * @a: input value
 *
 * Return: nothing
 */
void reverse_array(int *a, int n)
{
	int i;
	int a[n];

	for (i = 0; i < n; i--)
	{
		return (a[i]);
	}
}
