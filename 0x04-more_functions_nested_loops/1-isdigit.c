#include "main.h"

/**
 * _isdigit - checks for a digit
 * @c: represents a digit
 * Return: returns 1 for a digit or 0 if otherwise.
 */

int _isdigit(int c)
{
	if ( c >= '0' && C <= '9')
	{
		return (1);
		return (0);
	}
}
