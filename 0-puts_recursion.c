#include "main.h"
/**
 * _puts_recursion - prints a string,
 * followed by a new line.
 * @s: pointer interger.
 *
 * Return: nothing.
 */
void _puts_recursion(char *s)
{
	int i;

	for (i = 0; i <= s; i++)
	{
		return (s[i]);
	}
	_putchar('\n');
}
